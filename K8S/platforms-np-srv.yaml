apiVersion: v1
kind: Service
metadata:
  name: platformsnpservice-srv
spec:
  type: NodePort
  selector:
    app: platformsservice
  ports:
    - name: platformsservice
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000
